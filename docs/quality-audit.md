# Audit local des extractions

> Convention de version : les références comme `v0.3.1`, `v0.3.7` ou `v0.3.8` désignent la version interne du parseur et du format d'extraction, telle qu'elle est définie dans `src/analyseur_sudoc/unimarc.py` par `PARSER_VERSION`, et non la version du paquet Python déclarée dans `pyproject.toml`. Les dossiers `unimarc-v0.3.7` et `unimarc-v0.3.8` sont des lots d'extraction produits par `scripts/03_parse_unimarc.py`.

## Nouveautés v0.3.1 : enrichissements BnF et autorités IdRef
